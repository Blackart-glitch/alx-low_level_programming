#include "main.h"

/**
 * reset_to_98 - resets value to 98
 * main - check the code
 * @n: the pointer
 *
 * Return: Always 0.
 */
int _strlen(char *s)
{
	char y, count;

	y = *s;
	for (count = 0; str[count] != '\0'; ++count);
	return (count);
}
