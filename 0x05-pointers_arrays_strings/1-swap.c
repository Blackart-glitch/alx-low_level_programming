#include "main.h"

/**
 * swap_int - resets value to 98
 * @a: the pointer 1
 * @b: the pointer 2
 *
 * Return: returns result
 */

void swap_int(int *a, int *b)
{
	int *c;
	
	*c = NULL;
	*a = *c;
	*b = *a;
	*c = *b;
}

