#include <stdio.h>

/**
 * main - Entry point
 *@text - initializes variableq
 *
 * Return: Exit point
 */
int main(void)
{
char text[] = "\"Programming is like building a multilingual puzzle";
puts(text);
return (0);
}
