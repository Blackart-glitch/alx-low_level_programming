#include "main.h"
#include <stdio.h>

/**
 * _isupper - finds uppercase
 *@c:  varable to test
 *
 * Return: 1 (success) 0 (failed)
 */

int _isupper(int c)
{
	int i;

	for (i = 'A'; i <= 'Z'; i++)
	{
		if (a == i)
		{
		return (1);
		}
		else
		{
		return (0);
		}
	}
}
